{
  "name": "pug-simple-application",
  "version": "1.0.0",
  "description": "Simple application built using @yggdrasil architecture and 'pug' view template.",
  "license": "MIT",
  "author": "David Lopez",
  "scripts": {
    "clean": "gulp clean",
    "clean:all": "gulp clean:all",
    "compile": "gulp build",
    "start": "gulp start",
    "test": "npm run compile:test && mocha --exit dist/test/**/*.spec.js",
    "tslint": "tslint -p tsconfig.json -t verbose",
    "tslint:test": "tslint -p tsconfig.spec.json -t verbose"
  },
  "dependencies": {
    "@yggdrasil/mvc": "canary",
    "@yggdrasil/startup": "canary",
    "bootstrap": "^4.0.0",
    "jquery": "^3.3.1",
    "pug": "^2.0.0-rc.4"
  },
  "devDependencies": {
    "@yggdrasil/devs": "canary",
    "@yggdrasil/testing": "canary"
  }
}
