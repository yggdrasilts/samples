{
	"name": "pug-simple-application",
	"version": "1.0.0",
	"description": "Simple application built using @yggdrasilts architecture and 'pug' view template.",
	"license": "MIT",
	"author": "David Lopez",
	"scripts": {
		"clean": "gulp clean",
		"clean:all": "gulp clean:all",
		"compile": "gulp build",
		"compile:test": "gulp build:test",
		"start": "gulp start",
		"test": "gulp compile:test && nyc --require ts-node/register mocha --exit --recursive \"src/**/*.spec.ts\""
	},
	"dependencies": {
		"@yggdrasilts/core": "canary",
		"@yggdrasilts/mvc": "canary",
		"bootstrap": "^4.0.0",
		"jquery": "^3.3.1",
		"pug": "^2.0.0-rc.4"
	},
	"devDependencies": {
		"@yggdrasilts/devs": "canary",
		"@yggdrasilts/testing": "canary"
	}
}
